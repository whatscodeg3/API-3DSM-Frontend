
	const onChangeValorInserido = (e) => {
	  const novoValor = e.target.value;
	  setpaymentValue(novoValor);
	};

	const dinheiro = new Intl.NumberFormat('pt-BR', { 
		style: 'currency', 
		currency: 'BRL', 
		minimumFractionDigits: 2
	  }).format(paymentValue / 100);
	  console.log(dinheiro)

	const Texto = toString(dinheiro)


    							<Controller
								control={control}
								name="myInput"
								defaultValue={installment}
								render={({ field }) => (
									<InputText style={{ width: '400px' }} 
									prefix="R$" name={field.name} id={field.name} {...field}
									onChange={(e) => {
										field.onChange(e);
										setpaymentValue(e.target.value);
									}}
									value={installment}
									className={errors?.paymentValue && "input-error"}/>
									)
								}
							/>